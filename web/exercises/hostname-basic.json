{
  "id": "hostname-basic",
  "description": "Configure device hostname and save configuration",
  "deviceModel": "2960-switch",
  "commands": [
    "enable",
    "configure terminal",
    "hostname MyFirstSwitch",
    "end",
    "write memory"
  ],
  "validation": {
    "type": "goal-based",
    "assertions": [
      {
        "type": "hostname",
        "expected": "MyFirstSwitch",
        "message": "Hostname should be MyFirstSwitch"
      }
    ]
  }
}

